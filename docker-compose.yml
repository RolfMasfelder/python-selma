services:
  ollama:
    image: ollama/ollama:0.32.15
    container_name: selma-ollama
    environment:
      OLLAMA_CONTEXT_LENGTH: 65536
    restart: unless-stopped
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama

volumes:
  ollama_data:
